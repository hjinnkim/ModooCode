#include <stdio.h>

int main(){
    // char data type stores charaters
    char a;
    a = 'a';

    //a = a initially contains dummy value of NULL;

    printf("number of a : %d, charater of a : %c\n", a, a);

    return 0;
}