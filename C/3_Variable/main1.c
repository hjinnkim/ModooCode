#include <stdio.h>
int main(){
    int a;
    a = 127;
    printf("a = %o in octal number\n", a);
    printf("a = %d in decimal number\n", a);
    printf("a = %x in hexadecimal number\n", a);

    return 0;

}